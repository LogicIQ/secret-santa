---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: secretsanta.secrets.secret-santa.io
spec:
  group: secrets.secret-santa.io
  names:
    kind: SecretSanta
    listKind: SecretSantaList
    plural: secretsanta
    shortNames:
    - ss
    singular: secretsanta
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecretSanta is the Schema for the secretsantas API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecretSantaSpec defines the desired state of SecretSanta
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations to apply to the generated secret
                type: object
              dryRun:
                default: false
                description: DryRun enables validation mode without creating actual
                  secrets
                type: boolean
              generators:
                description: Generators define the secret value generators used in
                  the template
                items:
                  description: GeneratorConfig defines configuration for secret generators
                  properties:
                    config:
                      description: Config contains generator-specific configuration
                        parameters
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    name:
                      description: Name is the unique identifier for this generator
                        within the template
                      minLength: 1
                      type: string
                    type:
                      description: |-
                        Type specifies the generator type (e.g., random_password, tls_private_key)
                        Supported types: random_password, random_string, random_uuid, random_bytes,
                        random_integer, random_id, tls_private_key, tls_self_signed_cert,
                        tls_cert_request, tls_locally_signed_cert, crypto_aes_key, crypto_rsa_key,
                        crypto_ed25519_key, crypto_hmac
                      enum:
                      - random_password
                      - random_string
                      - random_uuid
                      - random_bytes
                      - random_integer
                      - random_id
                      - tls_private_key
                      - tls_self_signed_cert
                      - tls_cert_request
                      - tls_locally_signed_cert
                      - crypto_aes_key
                      - crypto_rsa_key
                      - crypto_ed25519_key
                      - crypto_hmac
                      minLength: 1
                      type: string
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              labels:
                additionalProperties:
                  type: string
                description: Labels to apply to the generated secret
                type: object
              media:
                description: Media specifies where to store the generated secret (defaults
                  to Kubernetes)
                properties:
                  config:
                    description: Config contains storage backend specific configuration
                      parameters
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  type:
                    description: |-
                      Type specifies the storage backend
                      Supported types: k8s, aws-secrets-manager, aws-parameter-store, azure-key-vault, gcp-secret-manager
                    enum:
                    - k8s
                    - aws-secrets-manager
                    - aws-parameter-store
                    - azure-key-vault
                    - gcp-secret-manager
                    minLength: 1
                    type: string
                required:
                - type
                type: object
              secretName:
                description: SecretName overrides the default secret name (defaults
                  to CR name)
                type: string
              secretType:
                default: Opaque
                description: SecretType sets the Kubernetes secret type
                type: string
              template:
                description: Template is the Go template string for generating secret
                  data
                minLength: 1
                type: string
            required:
            - generators
            - template
            type: object
          status:
            description: SecretSantaStatus defines the observed state of SecretSanta
            properties:
              conditions:
                description: Conditions represent the current state of the SecretSanta
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dryRunResult:
                description: DryRunResult contains the masked output from dry-run
                  executions
                properties:
                  executionTime:
                    format: date-time
                    type: string
                  generatorsUsed:
                    items:
                      type: string
                    type: array
                  maskedOutput:
                    type: string
                type: object
              lastGenerated:
                description: LastGenerated timestamp of the last successful secret
                  generation
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
